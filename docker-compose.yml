version: "3.7"

volumes:
  mongo_data:

services:
  all_moscow:
    build:
      context: ./Docker/Dockerfiles
      dockerfile: Dockerfile-all-Moscow
    restart: always
    volumes:
      - .:/source
    depends_on:
      - mongo
      - rabbit
  main:
    build:
      context: ./Docker/Dockerfiles
      dockerfile: Dockerfile-main
    restart: always
    volumes:
      - .:/source
    depends_on:
      - mongo
      - rabbit
  parser:
    build:
      context: ./Docker/Dockerfiles
      dockerfile: Dockerfile-parser
    restart: always
    volumes:
      - .:/source
    depends_on:
      - mongo
      - rabbit
  updates_manager:
    build:
      context: ./Docker/Dockerfiles
      dockerfile: Dockerfile-updates-manager
    restart: always
    volumes:
      - .:/source
    depends_on:
      - mongo
      - rabbit
  mongo:
    image: mongo:latest
    restart: always
    ports:
      - "127.0.0.1:27018:27017"
    volumes:
      - mongo_data:/data/db
      # - /mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
  rabbit:
    image: rabbitmq:3-management
    ports:
      - "127.0.0.1:5672:5672"
      - "127.0.0.1:15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=telegram_realty_admin
      - RABBITMQ_DEFAULT_PASS=realty_da_best
